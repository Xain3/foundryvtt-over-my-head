[project]
name = "foundryvtt-over-my-head"
version = "1.0.0"
description = "FoundryVTT Over My Head module"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["python_src/foundryvtt_over_my_head"]

[tool.hatch.build.targets.sdist]
include = ["python_src/foundryvtt_over_my_head"]
